// Print the board before each turn
  // Loop through each row
    //Loop through each space
        // print X, O or space
        // Print a/ between
      // Print a newline at the end
      // Pring a horizontal line between
